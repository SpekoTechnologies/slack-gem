require "spec_helper"

RSpec.describe Speko::Slack do
  it "has a version number" do
    expect(Speko::Slack::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
